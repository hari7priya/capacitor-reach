import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "org.uclahealth.ctrl.capreach",
  appName: "capreach",
  webDir: "build", // This is the directory where the web assets are located
};

export default config;
